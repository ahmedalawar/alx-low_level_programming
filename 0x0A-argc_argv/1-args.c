#include "main.h"

/**
*main - program that prints the number of arguments passed into it.
*@argc: number of arguments
*Return: always (0).
*/

int main(int argc)
{
	printf("%d\n", argc);
	return (0);
}
